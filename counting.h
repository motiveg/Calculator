#include "numberinput.h"

#ifndef counting_h
#define counting_h

double factorial(double);
double permute  (int, int);
double combine  (int, int);
    
void performFactorial(NumberInput*);
void performPermute  (NumberInput*);
void performCombine  (NumberInput*);

#endif /* counting_h */
